<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="co.myechelon.trial">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_la"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_la"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".post" />
        <activity android:name=".details" />
        <activity android:name=".CategoryActivity" />
        <activity android:name=".AllActivity" />
        <activity android:name=".AllEActivity" />

        <receiver android:name=".NotificationPublisher" />

        <activity android:name=".Main3Activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".colorActivity">

        </activity>
        <activity android:name=".PostCategroy"></activity>
    </application>

</manifest>